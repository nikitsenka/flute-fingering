{
  "name": "flute-fingering",
  "private": true,
  "description": "Static app: no build, no runtime dependencies. This file exists only so the checks in tools/ that need a browser have somewhere to record it.",
  "scripts": {
    "check": "node tools/check_i18n.js && node tools/check_songs.js && node tools/check_pitch.js && node tools/check_audio.js",
    "serve": "python3 -m http.server 8000"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
